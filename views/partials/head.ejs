<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    <title><%= typeof title !== 'undefined' ? title : 'Professional AI Prompts' %> | PromptLib</title>
    <meta name="description" content="<%= typeof description !== 'undefined' ? description : 'Curated professional AI prompts for ChatGPT, Midjourney, and Claude. Lifetime access.' %>">
    
    <meta property="og:type" content="website">
    <meta property="og:url" content="<%= config.APP_URL + path %>">
    <meta property="og:title" content="<%= typeof title !== 'undefined' ? title : 'PromptLib' %>">
    <meta property="og:description" content="<%= typeof description !== 'undefined' ? description : 'Access the world\'s best curated AI prompts.' %>">
    <meta property="og:image" content="<%= config.APP_URL %>/public/og-image.png">

    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="<%= typeof title !== 'undefined' ? title : 'PromptLib' %>">
    
    <script src="https://cdn.tailwindcss.com?plugins=typography,forms"></script>
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    <style>[x-cloak] { display: none !important; }</style>
</head>
<body class="h-full flex flex-col font-sans text-gray-900">
    <%- include('./nav') %>
    <% if (typeof path === 'undefined' || !path.startsWith('/admin')) { %>
        <%- include('./flash') %>
    <% } %>
    <main class="flex-grow">
