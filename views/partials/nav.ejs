<nav class="bg-white border-b border-gray-200" x-data="{ mobileMenuOpen: false }">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            
            <div class="flex">
                <div class="flex-shrink-0 flex items-center">
                    <a href="/" class="text-xl font-bold text-indigo-600 tracking-tight">PromptLib</a>
                </div>
                <div class="hidden sm:ml-8 sm:flex sm:space-x-8">
                    <a href="/library" class="border-transparent text-gray-500 hover:border-indigo-500 hover:text-indigo-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-bold transition">
                        Library
                    </a>
                    
                    <a href="/pricing" class="border-transparent text-gray-500 hover:border-indigo-500 hover:text-indigo-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-bold transition">
                        Pricing
                    </a>

                    <a href="/library/leaderboard" class="border-transparent text-gray-500 hover:border-indigo-500 hover:text-indigo-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-bold transition">
                        Community
                    </a>

                    <% if (locals.currentUser) { %>
                        <a href="/dashboard" class="border-transparent text-gray-500 hover:border-indigo-500 hover:text-indigo-600 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-bold transition">
                            Dashboard
                        </a>
                        <% if (currentUser.role === 'ADMIN') { %>
                            <a href="/admin" class="border-transparent text-indigo-500 hover:text-indigo-700 inline-flex items-center px-1 pt-1 border-b-2 text-sm font-black transition">
                                Admin
                            </a>
                        <% } %>
                    <% } %>
                </div>
            </div>

            <div class="hidden lg:flex flex-1 items-center justify-center px-12">
                <button 
                    @click="$dispatch('keydown', { key: 'k', metaKey: true, preventDefault: () => {} })" 
                    class="w-full max-w-xs flex items-center text-left text-gray-400 bg-gray-50 hover:bg-gray-100 ring-1 ring-gray-200 rounded-xl px-4 py-2 transition group"
                >
                    <svg class="h-4 w-4 mr-2 group-hover:text-indigo-500 transition" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>
                    <span class="text-xs flex-grow font-medium text-gray-400">Quick search...</span>
                    <kbd class="font-sans font-bold text-[10px] bg-white border border-gray-300 px-2 py-0.5 rounded shadow-sm text-gray-300">⌘K</kbd>
                </button>
            </div>

            <div class="hidden sm:ml-6 sm:flex sm:items-center sm:space-x-4">
                <% if (locals.currentUser) { %>
                    <div class="flex items-center gap-4">
                        <div class="flex flex-col items-end">
                            <span class="text-[9px] font-black uppercase tracking-[0.2em] text-gray-400">Account</span>
                            <span class="text-xs font-bold text-gray-900">u/<%= currentUser.username %></span>
                        </div>
                        
                        <form action="/logout" method="POST">
                            <input type="hidden" name="_csrf" value="<%= locals.csrfToken || '' %>">
                            <button type="submit" class="inline-flex items-center px-4 py-2 border border-gray-200 text-xs font-bold uppercase tracking-widest text-red-500 bg-white hover:bg-red-50 hover:border-red-200 rounded-xl shadow-sm transition active:scale-95">
                                Sign Out
                            </button>
                        </form>
                    </div>
                <% } else { %>
                    <a href="/login" class="text-gray-500 hover:text-gray-900 text-sm font-bold transition">Log in</a>
                    <a href="/register" class="inline-flex items-center px-6 py-2.5 border border-transparent text-sm font-black uppercase tracking-widest rounded-xl shadow-lg shadow-indigo-100 text-white bg-indigo-600 hover:bg-indigo-700 transition active:scale-95">
                        Join Free
                    </a>
                <% } %>
            </div>
            
            <div class="-mr-2 flex items-center sm:hidden">
                <button @click="mobileMenuOpen = !mobileMenuOpen" type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-gray-500 hover:bg-gray-100 focus:outline-none transition">
                    <svg class="h-6 w-6" x-show="!mobileMenuOpen" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" /></svg>
                    <svg class="h-6 w-6" x-show="mobileMenuOpen" x-cloak fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg>
                </button>
            </div>
        </div>
    </div>

    <div x-show="mobileMenuOpen" x-cloak x-transition class="sm:hidden border-t border-gray-100 bg-white">
        <div class="pt-4 pb-6 space-y-2 px-4">
            <a href="/library" class="block py-3 text-base font-bold text-gray-900 hover:text-indigo-600">Library Catalog</a>
            <a href="/pricing" class="block py-3 text-base font-bold text-gray-900 hover:text-indigo-600">Plans & Pricing</a>
            <a href="/library/leaderboard" class="block py-3 text-base font-bold text-gray-900 hover:text-indigo-600">Top Curators</a>
            
            <% if (locals.currentUser) { %>
                <div class="pt-4 mt-4 border-t border-gray-100">
                    <div class="flex items-center px-4 py-3 bg-gray-50 rounded-xl mb-4">
                        <div class="flex-shrink-0 h-10 w-10 rounded-full bg-indigo-100 flex items-center justify-center text-indigo-600 font-bold uppercase">
                            <%= currentUser.username[0] %>
                        </div>
                        <div class="ml-3">
                            <div class="text-base font-bold text-gray-800">u/<%= currentUser.username %></div>
                            <div class="text-xs text-gray-500"><%= currentUser.email %></div>
                        </div>
                    </div>
                    <a href="/dashboard" class="block py-3 px-4 text-base font-bold text-indigo-600 hover:bg-gray-50 rounded-lg">My Dashboard</a>
                    <form action="/logout" method="POST" class="mt-2">
                        <input type="hidden" name="_csrf" value="<%= locals.csrfToken || '' %>">
                        <button class="w-full text-left py-3 px-4 text-base font-bold text-red-500 hover:bg-red-50 rounded-lg transition">Sign Out</button>
                    </form>
                </div>
            <% } else { %>
                <div class="grid grid-cols-2 gap-4 pt-6">
                    <a href="/login" class="flex justify-center items-center py-3 border border-gray-200 rounded-xl text-sm font-bold text-gray-700">Log In</a>
                    <a href="/register" class="flex justify-center items-center py-3 bg-indigo-600 rounded-xl text-sm font-bold text-white shadow-lg">Sign Up</a>
                </div>
            <% } %>
        </div>
    </div>
</nav>

<%- include('./command-bar') %>
